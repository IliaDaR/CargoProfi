# 🚛 ПОЛНЫЙ QA-АУДИТ ПРОЕКТА NUMINO (CargoProfi)

**Дата:** 02.06.2026  
**Версия проекта:** 1.0.0+1  
**Цель аудита:** Полная проверка лендинга, кабинетов владельца и приложения водителя с выявлением всех ошибок, недочётов и нереализованного функционала.

---

## 📌 СОДЕРЖАНИЕ

1. [ГЛОБАЛЬНАЯ АРХИТЕКТУРА ПРОЕКТА](#1-глобальная-архитектура-проекта)
2. [АУДИТ ЛЕНДИНГА (website/)](#2-аудит-лендинга)
   - 2.1 HTML-структура
   - 2.2 CSS-стили
   - 2.3 JavaScript-функционал
   - 2.4 Доступность (Accessibility)
   - 2.5 SEO и мета-теги
   - 2.6 Безопасность
   - 2.7 Производительность
3. [АУДИТ КАБИНЕТА ВЛАДЕЛЬЦА (Flutter Web)](#3-аудит-кабинета-владельца)
   - 3.1 Авторизация и сессии
   - 3.2 Дашборд
   - 3.3 Управление автомобилями
   - 3.4 Управление рейсами
   - 3.5 Управление расходами
   - 3.6 Расчёт зарплаты
   - 3.7 Суперадмин-панель
   - 3.8 Хранилище данных
4. [АУДИТ ПРИЛОЖЕНИЯ ВОДИТЕЛЯ](#4-аудит-приложения-водителя)
   - 4.1 Основной экран
   - 4.2 Активный рейс
   - 4.3 GPS-трекинг
   - 4.4 Добавление расходов
5. [АУДИТ FIREBASE BACKEND](#5-аудит-firebase-backend)
6. [СВОДНАЯ ТАБЛИЦА ОШИБОК](#6-сводная-таблица-ошибок)
7. [ЗАКЛЮЧЕНИЕ И РЕКОМЕНДАЦИИ](#7-заключение-и-рекомендации)

---

# 1. ГЛОБАЛЬНАЯ АРХИТЕКТУРА ПРОЕКТА

## 1.1 Общее описание

Numino — SaaS-сервис "Рабочий кабинет перевозчика". Проект состоит из 4 компонентов:

| Компонент | Технология | Назначение |
|-----------|-----------|------------|
| **Лендинг** | HTML + CSS + JS | Промо-сайт, привлечение клиентов, вход/регистрация |
| **Кабинет владельца** | Flutter Web | Дашборд, управление парком, рейсами, расходами, зарплатой |
| **Приложение водителя** | Flutter (Android) | Начало/завершение рейсов, GPS-трекинг, добавление расходов |
| **Backend** | Firebase (Functions + Firestore + Storage) | Облачная логика, БД, хранение файлов |

## 1.2 Проблемы архитектуры

### 🔴 КРИТИЧЕСКАЯ: Две раздельные БД пользователей
- **Лендинг** использует `localStorage` с ключом `numino_users` (JS)
- **Flutter** использует `SharedPreferences` с ключом `users` (Dart)
- Пользователь, зарегистрировавшийся на лендинге, НЕ МОЖЕТ войти в кабинет Flutter
- Демо-пользователи разные:
  - Лендинг: `admin@numino.ru` (admin), `owner@numino.ru` (owner)
  - Flutter: `admin@numino.ru` (superadmin), `owner@numino.ru` (owner), `driver@numino.ru` (driver)
- **Рекомендация:** Синхронизировать БД или использовать единый Firebase Auth

### 🔴 КРИТИЧЕСКАЯ: Дыра безопасности (Auth Bypass)
- В `main.dart` (строки 69-78) URL-параметры `?role=admin&email=x&name=x` создают сессию БЕЗ проверки пароля
- Любой может добавить в адресную строку `admin/index.html?role=superadmin&email=hacker@mail.ru` и получить полный доступ
- **Рекомендация:** Удалить или защитить этот код — всегда проверять пароль или Firebase токен

### 🟡 Две точки входа для водителя
- `main.dart` — Web-версия с RoleScreen
- `main_driver.dart` — Android-версия (заглушка)
- Код дублируется, нет общего DI/провайдера

---

# 2. АУДИТ ЛЕНДИНГА

## 2.1 HTML-структура (`website/index.html` — 337 строк)

### ✅ Что реализовано:
- [x] DOCTYPE html, lang="ru"
- [x] Charset UTF-8, viewport meta
- [x] Description, favicon, preconnect для Google Fonts
- [x] Header с логотипом, навигацией, бургер-меню
- [x] Hero секция с заголовком, подзаголовком, статистикой, кнопками
- [x] Features секция (6 карточек) с каруселью
- [x] How секция (4 шага) с каруселью
- [x] Pricing секция (3 тарифа: Старт, Бизнес, Корпоративный)
- [x] Download секция (APK для водителя)
- [x] Testimonials секция (отзыв)
- [x] Contact секция с формой
- [x] Footer с навигацией
- [x] Login modal с формой входа/регистрации
- [x] `<main>` landmark (ИСПРАВЛЕНО)
- [x] Все кнопки с `type="button"` (ИСПРАВЛЕНО)
- [x] Лейблы привязаны к инпутам через `for`/`id` (ИСПРАВЛЕНО)
- [x] Honeypot в HTML (ИСПРАВЛЕНО)

### ❌ Что не реализовано / что нужно исправить:

| # | Проблема | Строки | Серьёзность | Статус |
|---|---------|--------|-------------|--------|
| 1 | Нет `<meta name="theme-color">` для мобильных браузеров | — | 🟡 Средняя | Не исправлено |
| 2 | Нет `<link rel="manifest">` для PWA | — | 🟡 Средняя | Не исправлено |
| 3 | Нет `<meta property="og:..."` для соцсетей | — | 🟢 Низкая | Не исправлено |
| 4 | Контактная форма редиректит на `https://numino.ru/thanks` — домен может не существовать | 297 | 🟡 Средняя | Не исправлено |
| 5 | Нет `<noscript>` с сообщением для пользователей без JS | — | 🟢 Низкая | Не исправлено |
| 6 | Нет кэширующих заголовков или Service Worker | — | 🟡 Средняя | Не исправлено |
| 7 | Нет микроразметки (JSON-LD) для SEO | — | 🟢 Низкая | Не исправлено |

## 2.2 CSS-стили (`website/css/style.css` — 189 строк)

### ✅ Что реализовано:
- [x] CSS-переменные (:root) для темизации
- [x] Система кнопок (btn, btn--primary, btn--outline, btn--sm, btn--lg)
- [x] Адаптивность через медиа-запросы (3 брейкпоинта: 900px, 800px, 500px)
- [x] Карусель с анимацией
- [x] Модальное окно с анимацией появления
- [x] Скролл-анимации для карточек
- [x] Стили для тёмной темы контактов (серый фон)
- [x] Кнопка "Наверх" (scroll-top)

### ❌ Проблемы CSS:

| # | Проблема | Серьёзность | Статус |
|---|---------|-------------|--------|
| 1 | Нет `prefers-reduced-motion` — пользователи с вестибулярными нарушениями не могут отключить анимации | 🟡 Средняя | Не исправлено |
| 2 | Нет `prefers-color-scheme` — нет тёмной темы | 🟢 Низкая | Не исправлено |
| 3 | Высота `.hero` не использует `min-height: 100vh` — на больших экранах пустота под hero | 🟢 Низкая | Не исправлено |
| 4 | `.nav` на мобильных имеет `display:none` в CSS, но JS управляет через класс `.open` — корректно | ✅ Ок | Проверено |
| 5 | Нет стилей для `focus-visible` — пользователи с клавиатурой не видят фокус | 🟡 Средняя | Не исправлено |

## 2.3 JavaScript-функционал (`website/js/main.js` — 336 строк)

### ✅ Что реализовано:
- [x] Бургер-меню с открытием/закрытием
- [x] Кнопка "Наверх" с плавным скроллом
- [x] Модальное окно входа (open/close, Escape, focus trap)
- [x] Переключение между входом и регистрацией
- [x] localStorage-based auth (getUsers, saveUsers, seed users)
- [x] Валидация email
- [x] XSS-санитайзер
- [x] Карусель (createCarousel) с автопрокруткой, точками, свайпом
- [x] Скролл-анимации появления элементов
- [x] Плавный скролл к якорям
- [x] Сохранение тикетов в localStorage (для админки)
- [x] Синхронизация с Flutter через `flutter.numino_tickets`

### ❌ Проблемы JavaScript:

| # | Проблема | Строки | Серьёзность | Статус |
|---|---------|--------|-------------|--------|
| 1 | **setInterval утечка памяти** — `startAuto()` запускает `setInterval`, но нет `clearInterval` при `beforeunload` или `pagehide` | 239-240 | 🟡 Средняя | Не исправлено |
| 2 | **XSS-санитайзер неполный** — заменяет `<>`, `on\w+=`, `javascript:` — НО не защищает от: `onerror` через пробел (`on error`), `eval()`, `data:`, `vbscript:`, `&#` (html entities), Base64-инъекций | 316-317 | 🔴 Высокая | Не исправлено |
| 3 | **Скролл-анимация ломает UI при отказе JS** — `opacity: 0` устанавливается сразу в цикле. Если JS упадёт после этой строки, элементы останутся невидимыми | 286 | 🟡 Средняя | Не исправлено |
| 4 | **Два window.addEventListener('scroll')** — можно объединить для производительности | 33, 37 | 🟢 Низкая | Не исправлено |
| 5 | **Пароли в открытом виде в localStorage** — хранятся как plain text | 146 | 🔴 Высокая | Не исправлено |
| 6 | **setTimeout 600ms перед редиректом** — задержка визуально заметна, лучше использовать Promise + анимацию | 169 | 🟢 Низкая | Не исправлено |
| 7 | **Нет обработки ошибок fetch (нет fetch вообще)** — все запросы синхронные, но для кода нет обработки сбоев localStorage | — | 🟢 Низкая | Не исправлено |
| 8 | **scrollTopBtn добавляется через createElement** — на странице есть пустой `<button>` без type до вызова sanitize | 26-31 | 🟢 Низкая | Исправлено (добавлен type) |
| 9 | **Карусель — нет остановки при `visibilitychange`** — если пользователь переключил вкладку, таймер продолжает тикать | 239 | 🟢 Низкая | Не исправлено |
| 10 | **Контактная форма не проверяет honeypot на клиенте** — поле `_honey` добавляется, но JS не проверяет его перед отправкой | — | 🟢 Низкая | Не исправлено |

## 2.4 Доступность (Accessibility)

### ✅ Что реализовано:
- [x] `role="region"` с `aria-label` у каруселей
- [x] `aria-live="polite"` у каруселей
- [x] `aria-modal="true"` и `role="dialog"` у модалки
- [x] `aria-label` у кнопок карусели и бургер-меню
- [x] `aria-labelledby="modalTitle"` у модалки
- [x] Focus trap в модальном окне
- [x] `sr-only` класс для скринридеров
- [x] `alt` у всех изображений
- [x] `<main>` элемент (ИСПРАВЛЕНО)

### ❌ Проблемы доступности:

| # | Проблема | Серьёзность | Статус |
|---|---------|-------------|--------|
| 1 | Нет `aria-current="page"` в навигации | 🟢 Низкая | Не исправлено |
| 2 | Нет `aria-controls` для бургер-меню | 🟢 Низкая | Не исправлено |
| 3 | Нет `aria-expanded` для бургер-меню | 🟡 Средняя | Не исправлено |
| 4 | Цветовой контраст: серый текст на белом (#64748b на #fff) — проверка WCAG AA | 🟡 Средняя | Требует проверки |
| 5 | Нет skip-to-content ссылки для клавиатурной навигации | 🟡 Средняя | Не исправлено |
| 6 | Нет `role="tablist"`/`role="tab"` в тарифах | 🟢 Низкая | Не исправлено |
| 7 | Эмодзи в статистике и тарифах могут не озвучиваться скринридерами | 🟢 Низкая | Не исправлено |

## 2.5 SEO и мета-теги

### ✅ Что реализовано:
- [x] `<title>` — "Numino — Рабочий кабинет перевозчика"
- [x] `<meta name="description">` — описание сервиса
- [x] `<meta name="viewport">` — адаптивность
- [x] `<meta charset="UTF-8">`
- [x] Favicon (SVG)

### ❌ Что отсутствует:

| # | Отсутствует | Серьёзность |
|---|------------|-------------|
| 1 | Open Graph (og:title, og:description, og:image, og:url) | 🟡 Средняя |
| 2 | Twitter Card (twitter:card, twitter:title) | 🟢 Низкая |
| 3 | JSON-LD structured data (Organization, Product, SoftwareApplication) | 🟡 Средняя |
| 4 | Sitemap.xml | 🟢 Низкая |
| 5 | Robots.txt | 🟢 Низкая |
| 6 | Canonical URL | 🟢 Низкая |

## 2.6 Безопасность лендинга

### ❌ Проблемы:

| # | Проблема | Серьёзность | Статус |
|---|---------|-------------|--------|
| 1 | **Пароли в открытом виде** в localStorage | 🔴 Критическая | Не исправлено |
| 2 | **Нет CSP (Content-Security-Policy)** через `<meta>` или заголовок | 🔴 Высокая | Не исправлено |
| 3 | **Форма обратной связи отправляется через FormSubmit** — пароль/токен не скрыт, но FormSubmit использует серверную обработку | 🟡 Средняя | Не исправлено |
| 4 | **Нет referrerpolicy** (есть `no-referrer` в `<meta>`) | ✅ Ок | Проверено |
| 5 | **XSS-санитайзер неполный** (см. JS проблемы) | 🔴 Высокая | Не исправлено |

## 2.7 Производительность лендинга

### ✅ Что хорошо:
- [x] Google Fonts с preconnect
- [x] CSS не минифицирован, но один файл
- [x] JS один файл, без внешних зависимостей
- [x] Нет тяжёлых библиотек (jQuery, Bootstrap)
- [x] Изображения в SVG — лёгкие и масштабируемые

### ❌ Что можно улучшить:

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | Нет минификации CSS и JS | 🟢 Низкая |
| 2 | Нет кэширования через Cache-Control | 🟡 Средняя |
| 3 | Google Fonts загружаются синхронно (render-blocking) | 🟡 Средняя |
| 4 | Нет lazy-loading для изображений (loading="lazy") | 🟢 Низкая |
| 5 | Нет preload для критических ресурсов | 🟢 Низкая |

---

# 3. АУДИТ КАБИНЕТА ВЛАДЕЛЬЦА

## 3.1 Авторизация и сессии (`role_screen.dart`, `auth_provider.dart`, `firebase_auth_service.dart`)

### ✅ Что реализовано:
- [x] Выбор роли (Владелец / Водитель)
- [x] Диалог входа/регистрации
- [x] Fallback: если Firebase недоступен — локальная регистрация
- [x] Сохранение сессии в SharedPreferences
- [x] Восстановление сессии при перезагрузке страницы
- [x] AuthProvider с ChangeNotifier

### ❌ Проблемы:

| # | Проблема | Файл | Серьёзность | Статус |
|---|---------|------|-------------|--------|
| 1 | **Auth Bypass через URL** — URL-параметры создают сессию без пароля | main.dart:69-78 | 🔴 Критическая | Не исправлено |
| 2 | **Firebase Auth Service не реализован** — судя по коду, это заглушка, при вызове кидает исключение | firebase_auth_service.dart | 🔴 Высокая | Не исправлено |
| 3 | **Пароли в открытом виде** в SharedPreferences | local_storage.dart:53-57 | 🔴 Критическая | Не исправлено |
| 4 | **RoleScreen требует fireAuth параметр** — но Firebase Auth Service — заглушка, всегда fallback на localStorage | role_screen.dart:92-102 | 🟡 Средняя | Не исправлено |
| 5 | **После логина — Navigator.pushReplacement использует user из замыкания, который может не совпадать** | role_screen.dart:121-122 | 🟡 Средняя | Не исправлено |

## 3.2 Дашборд владельца (`owner_dashboard_screen.dart` — 111 строк)

### ✅ Что реализовано:
- [x] 5 вкладок: Дашборд, Машины, Рейсы, Расходы, Зарплата
- [x] Статистика: всего машин, в рейсе, свободны, количество рейсов, доход
- [x] Адаптивная навигация (Rail + BottomBar)
- [x] Кнопка уведомлений
- [x] Профиль пользователя и кнопка выхода
- [x] Logout с редиректом на RoleScreen (НО: на Web нужен `/`)

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **Логаут редиректит на RoleScreen, а не на `/`** — на Flutter Web после выхода пользователь видит экран выбора роли, а не лендинг | 53-54 | 🟡 Средняя |
| 2 | **Уведомления не работают** — NotificationService.show() не привязан к реальным событиям | — | 🟡 Средняя |
| 3 | **Watch на LocalStorage** — `context.watch<LocalStorage>()` может вызывать лишние ребилды | 30 | 🟢 Низкая |
| 4 | **Нет pull-to-refresh** для обновления данных | — | 🟢 Низкая |
| 5 | **Нет импорта `dart:html` для Web-логаута** (используется Navigator.pushAndRemoveUntil, не `html.window.location.href`) | 54 | 🟡 Средняя |

## 3.3 Управление автомобилями (`vehicles_screen.dart` — 99 строк)

### ✅ Что реализовано:
- [x] Список машин (марка, модель, госномер, статус)
- [x] Добавление машины через диалог
- [x] Удаление машины
- [x] Список водителей (связаны с владельцем)
- [x] Добавление/удаление водителя

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **Удаление машины не работает** — `store.removeVehicle(e.key)` использует индекс из VehicleProvider, а удаляет из LocalStorage — разные списки | 83 | 🔴 Высокая |
| 2 | **Hardcoded ownerId = 'local'** вместо currentUser | — | 🟡 Средняя |
| 3 | **Нет редактирования машины** — только добавление и удаление | — | 🟡 Средняя |
| 4 | **Нет валидации госномера** — можно ввести что угодно | — | 🟢 Низкая |
| 5 | **Диалог добавления не очищает поля после закрытия** | — | 🟢 Низкая |

## 3.4 Управление рейсами (`trips_screen.dart` — 85 строк)

### ✅ Что реализовано:
- [x] Список рейсов с фильтрацией по статусу
- [x] Поиск по маршруту или описанию груза
- [x] Адаптивное отображение (DataTable для десктопа, ListView для мобильных)
- [x] Кнопка генерации PDF путевого листа
- [x] Отображение дохода, пробега, дат

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **`_driverName()` вызывает `context.read<LocalStorage>()` на каждый рендер строки** — производительность | — | 🟡 Средняя |
| 2 | **Нет пагинации** — при 100+ рейсах всё будет тормозить | — | 🟡 Средняя |
| 3 | **Фильтр по статусу использует enum .name** — хрупко, зависит от английских названий | — | 🟢 Низкая |
| 4 | **Нет возможности редактировать рейс** | — | 🟢 Низкая |
| 5 | **PDF-генерация не реализована на клиенте** — вызов, вероятно, падает с ошибкой (заглушка) | — | 🟡 Средняя |

## 3.5 Управление расходами (`expenses_screen.dart` — 95 строк)

### ✅ Что реализовано:
- [x] Список расходов по выбранному водителю
- [x] Итоговая сумма по категориям
- [x] Thumbnails фото чеков (base64 + network)
- [x] Кнопка "CSV" для экспорта

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **`Printing.sharePdf(bytes: csv, ...)` передаёт CSV в sharePdf** — функция ожидает PDF, вызов упадёт или сохранит мусор | 63 | 🔴 Высокая |
| 2 | **`const Base64Decoder()` устарел** — может не работать на новых Dart SDK | — | 🟡 Средняя |
| 3 | **Нет фильтра по датам** — все расходы за всё время | — | 🟡 Средняя |
| 4 | **Нет возможности удалить расход** | — | 🟢 Низкая |
| 5 | **Нет фото preview в полный размер** | — | 🟢 Низкая |

## 3.6 Расчёт зарплаты (`salary_screen.dart` — 128 строк)

### ✅ Что реализовано:
- [x] Выбор водителя из списка
- [x] Выбор периода (начало/конец)
- [x] Правило: процент от дохода или фиксированная сумма за рейс
- [x] Кнопка "Рассчитать зарплату"
- [x] История выплат
- [x] Статусы: calculated / paid / cancelled
- [x] Кнопка CSV-экспорта
- [x] Сохранение правил и платежей

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **`store.saveSalaryRules()` вызывается дважды подряд** | 50-51 | 🟢 Низкая |
| 2 | **CSV-экспорт использует `Printing.sharePdf()`** — та же проблема, что в расходах | 108-109 | 🔴 Высокая |
| 3 | **Нет подтверждения перед изменением статуса на "Оплачено"** | 116-123 | 🟢 Низкая |
| 4 | **Нет деактивации кнопки расчёта во время загрузки** | — | 🟢 Низкая |
| 5 | **Dropdown водителей показывает uid, если нет displayName** — сырые данные | 86 | 🟢 Низкая |

## 3.7 Суперадмин-панель (`superadmin_screen.dart` — 197 строк)

### ✅ Что реализовано:
- [x] Список владельцев с поиском
- [x] Добавление владельца
- [x] Блокировка/разблокировка пользователя
- [x] Управление тарифами владельцев
- [x] Статистика (владельцы, водители, машины, рейсы, выручка)
- [x] График выручки по месяцам (простая визуализация)
- [x] Тикеты (обращения с лендинга) — просмотр и закрытие
- [x] Логи действий администратора
- [x] Выход из аккаунта

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **Логаут не редиректит на `/`** — только `setCurrentUser(null)`, пользователь остаётся на странице | 35 | 🟡 Средняя |
| 2 | **Тикеты читаются из SharedPreferences** — но на лендинге они пишутся и в `localStorage` и в `flutter.numino_tickets` через JS. SharedPreferences на Flutter Web не видит напрямую `localStorage` — **синхронизация не работает** | 220-229 | 🔴 Высокая |
| 3 | **Нет подтверждения перед блокировкой пользователя** | 77 | 🟢 Низкая |
| 4 | **Фильтр поиска применяется дважды** — в `filtered` и в `.where` внутри билдера | 31, 73 | 🟢 Низкая |
| 5 | **График выручки привязан к `.name` статуса** — хрупко | 114 | 🟢 Низкая |
| 6 | **Logs хранятся только в памяти** — при перезагрузке страницы теряются | 15 | 🟡 Средняя |

## 3.8 Хранилище данных (`local_storage.dart` — 241 строка)

### ✅ Что реализовано:
- [x] init() с SharedPreferences
- [x] Seed демо-данных при первом запуске
- [x] CRUD для: Vehicles, Trips, Expenses, Drivers, SalaryRules, SalaryPayments, Users
- [x] Сохранение/загрузка текущего пользователя
- [x] Поиск пользователя по email/паролю
- [x] Сохранение тикетов
- [x] Тарифы для владельцев
- [x] 5 публичных save-методов

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **`_tripToMap()` сохраняет `'track': []`** — GPS-треки теряются при сериализации | 161 | 🟡 Средняя |
| 2 | **`findUser()` сравнивает пароли в открытом виде** | 84 | 🔴 Критическая |
| 3 | **`saveTickets()` читает тикеты из `numino_tickets`, а сохраняет в `tickets`** — разные ключи! Данные теряются | 237-239 | 🔴 Высокая |
| 4 | **`addTicket` пишет в ключ `tickets`, а `tickets` геттер читает из `numino_tickets`** — разные ключи! | 231-234, 223 | 🔴 Высокая |
| 5 | **Нет инкапсуляции** — все списки публичные (`final List<Trip> trips`) | 26-33 | 🟡 Средняя |
| 6 | **Нет синхронизации с Firebase** — всё локально | — | 🟡 Средняя |

---

# 4. АУДИТ ПРИЛОЖЕНИЯ ВОДИТЕЛЯ

## 4.1 Основной экран (`driver_home_screen.dart` — 355 строк)

### ✅ Что реализовано:
- [x] Форма нового рейса (описание груза, маршрут)
- [x] Кнопка "Начать рейс" (зелёная, крупная)
- [x] Получение GPS-координат через Geolocator
- [x] История рейсов водителя
- [x] Кнопка логаута с редиректом на `/`
- [x] Проверка свободных машин
- [x] SnackBar ошибок при отсутствии машин
- [x] Активный рейс (ActiveTripScreen)

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **При создании рейса используется `_cargoCtrl.text` после очистки** — `_cargoCtrl.clear()` вызывается ДО вызова `_checkActive()` и ДО `NotificationService.tripStarted()` — **уведомление уходит с пустыми данными** | 81, 84-89 | 🔴 Высокая |
| 2 | **Дублирование кода рейса** — `Trip` создаётся дважды с разными данными (строки 74-80 и 84-89) | 74-89 | 🟡 Средняя |
| 3 | **Нет проверки наличия активного рейса при старте** — можно начать второй рейс, если предыдущий не завершён (но в БД это допустимо) | — | 🟡 Средняя |
| 4 | **Нет ограничения длины описания/маршрута** — можно ввести гигантский текст | — | 🟢 Низкая |
| 5 | **Geolocator timeout 10 секунд — если GPS не ловит, пользователь ждёт 10 секунд** | 66 | 🟡 Средняя |

## 4.2 Активный рейс (`ActiveTripScreen` — строки 138-355)

### ✅ Что реализовано:
- [x] Таймер обратного отсчёта (часы:минуты:секунды)
- [x] Статистика (расходы, количество чеков)
- [x] Кнопка "Добавить расход"
- [x] Кнопка "Завершить рейс"
- [x] Диалог завершения с пробегом и доходом
- [x] Расчёт пробега по GPS-треку (формула гаверсинуса)
- [x] GPS-трекинг каждые 30 секунд
- [x] Уведомления о начале/завершении рейса

### ❌ Проблемы:

| # | Проблема | Строки | Серьёзность |
|---|---------|--------|-------------|
| 1 | **GPS-трекинг не синхронизируется с рейсом** — `_track` хранится в памяти и НЕ сохраняется в `store.trips[idx]` при завершении. `'track': []` в сериализации. Треки теряются. | 161, 277 | 🔴 Высокая |
| 2 | **Haversine формула дублируется** — есть в `utils/distance.dart` (правильная) и в `driver_home_screen.dart` (своя реализация, использует `3.14159` вместо `math.pi` — потеря точности) | 345-351 | 🟡 Средняя |
| 3 | **Дубликат файла/класса** — `import 'package:flutter/material.dart';` импортирован дважды | 1, 9 | 🟢 Низкая |
| 4 | **После завершения рейса — Navigator.pushReplacement создаёт новый экземпляр DriverHomeScreen** — теряется состояние | 282 | 🟡 Средняя |
| 5 | **Нет подтверждения перед завершением — диалог есть, но без валидации полей** | 250-288 | 🟢 Низкая |
| 6 | **Если пользователь не ввёл доход — income будет null, показывается 0 ₽** | 131 | 🟢 Низкая |

## 4.3 GPS-трекинг

### ✅ Что реализовано:
- [x] Запрос координат при старте рейса
- [x] Таймер GPS-точек каждые 30 секунд
- [x] Расчёт пробега по массиву точек

### ❌ Проблемы:

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | **Нет фонового GPS-трекинга** — если пользователь свернул приложение (на Android), трекинг остановится | 🟡 Средняя |
| 2 | **Нет буферизации оффлайн-точек** — при обрыве связи точки теряются | 🟡 Средняя |
| 3 | **Нет отображения карты с треком** — пользователь не видит где ехал | 🟢 Низкая |
| 4 | **30 секунд — редкий интервал для GPS** — стандарт для грузоперевозок 60 сек, но можно сделать настраиваемым | 🟢 Низкая |

## 4.4 Добавление расходов (`_addExpense()` — строки 175-237)

### ✅ Что реализовано:
- [x] Выбор категории (8 категорий: топливо, стоянка, ремонт, и т.д.)
- [x] Сумма, описание
- [x] Фото чека (камера + галерея)
- [x] Base64 хранение фото
- [x] GPS-координаты прикрепляются автоматически

### ❌ Проблемы:

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | **Фото хранятся как base64 в SharedPreferences** — при нескольких фото память быстро переполнится | 🟡 Средняя |
| 2 | **Нет preview фото перед сохранением (есть thumbnail)** — thumbnail 120px высотой | 🟢 Низкая |
| 3 | **Нет подтверждения успешного сохранения (SnackBar есть)** — ✅ Ок | 🟢 Ок |
| 4 | **Нет кнопки "Сканировать QR код чека"** — фича не реализована | 🟢 Низкая |

---

# 5. АУДИТ FIREBASE BACKEND

## 5.1 Cloud Functions (`functions/src/index.ts`)

### ✅ Что реализовано (13 функций):
1. `startTrip` — начать рейс
2. `addTrackPoint` — добавить GPS-точку
3. `addTrackPointsBatch` — массив GPS-точек
4. `endTrip` — завершить рейс
5. `getMyTrips` — список рейсов водителя
6. `addExpense` — добавить расход
7. `getTripExpenses` — расходы по рейсу
8. `getDriverExpensesReport` — отчёт по расходам
9. `generateWaybill` — PDF путевого листа (через pdfkit)
10. `setSalaryRule` — задать правило зарплаты
11. `getSalaryRule` — получить правило зарплаты
12. `calculateSalary` — рассчитать зарплату
13. `getSalaryHistory` — история расчётов

### ❌ Проблемы:

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | **Все функции локальны на Flutter — Firebase Functions не используются** — весь код на клиенте | 🟡 Средняя |
| 2 | **Нет проверки подлинности Firebase Auth в функциях** — context.auth не валидируется в некоторых функциях | 🟡 Средняя |
| 3 | **Деплой не настроен** — нет `.firebaserc`, нет привязки к проекту Firebase | 🔴 Высокая |

## 5.2 Firestore Rules (`firestore.rules` — 172 строки)

### ✅ Что реализовано:
- [x] Role-based доступ
- [x] Owner может читать/писать свои данные
- [x] Driver — только свои рейсы и расходы
- [x] Проверка на аутентификацию

### ❌ Проблемы:

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | **Правила написаны хорошо, но не протестированы** — нет тестового прогона | 🟢 Низкая |
| 2 | **Нет rate limiting** — злоумышленник может спамить запросами | 🟡 Средняя |

## 5.3 Storage Rules (`storage.rules`)

### ✅ Что реализовано:
- [x] Только аутентифицированные пользователи
- [x] Ограничение по типам файлов (image/*)
- [x] Максимальный размер 10 MB

### ❌ Проблем: не обнаружено.

---

# 6. СВОДНАЯ ТАБЛИЦА ОШИБОК

## 🔴 КРИТИЧЕСКИЕ (необходимо исправить немедленно)

| # | Описание | Компонент | Файл |
|---|---------|-----------|------|
| C1 | Auth Bypass — URL-параметры создают сессию без пароля | Flutter Web | main.dart:69-78 |
| C2 | Пароли в открытом виде (localStorage + SharedPreferences) | Лендинг + Flutter | main.js:146, local_storage.dart:84 |
| C3 | Две раздельные БД пользователей — вход только в одной системе | Глобально | main.js + local_storage.dart |
| C4 | Тикеты пишутся в разные ключи (numino_tickets vs tickets) | Flutter | local_storage.dart:223,234,239 |
| C5 | CSV-экспорт вызывает sharePdf() для CSV — краш или мусор | Flutter | expenses_screen.dart:63, salary_screen.dart:108 |

## 🟡 ВЫСОКИЕ (срочно)

| # | Описание | Компонент | Файл |
|---|---------|-----------|------|
| H1 | Firebase Auth Service — заглушка, всегда падает | Flutter | firebase_auth_service.dart |
| H2 | XSS-санитайзер неполный — пропускает векторы атак | Лендинг | main.js:316-317 |
| H3 | GPS-треки не сохраняются — `'track': []` в сериализации | Flutter | local_storage.dart:161 |
| H4 | Удаление машины не работает — неверный индекс | Flutter | vehicles_screen.dart:83 |
| H5 | При создании рейса уведомление уходит с пустыми данными | Flutter | driver_home_screen.dart:81 |
| H6 | Haversine формула дублируется с потерей точности | Flutter | driver_home_screen.dart:345-351 |
| H7 | Логаут суперадмина не редиректит на `/` | Flutter | superadmin_screen.dart:35 |
| H8 | WASM MIME-типы — start.bat не отдаёт .wasm правильно | Лендинг | start.bat |
| H9 | Деплой не настроен — нет .firebaserc | Глобально | — |
| H10 | Нет CSP (Content-Security-Policy) | Лендинг | index.html |

## 🟡 СРЕДНИЕ

| # | Описание | Компонент |
|---|---------|-----------|
| M1 | Scroll-анимация делает элементы невидимыми при ошибке JS | Лендинг |
| M2 | setInterval утечка памяти (карусель) | Лендинг |
| M3 | Нет focus-visible стилей | Лендинг |
| M4 | Нет skip-to-content ссылки | Лендинг |
| M5 | Нет prefers-reduced-motion | Лендинг |
| M6 | Google Fonts render-blocking | Лендинг |
| M7 | Дубликат фильтра поиска в суперадмине | Flutter |
| M8 | Нет пагинации в списках | Flutter |
| M9 | Нет pull-to-refresh | Flutter |
| M10 | role_screen использует user из замыкания после popUntil | Flutter |
| M11 | Фото в base64 — переполнение памяти | Flutter |
| M12 | Нет фонового GPS-трекинга | Flutter |

## 🟢 НИЗКИЕ

| # | Описание |
|---|---------|
| L1 | Нет Open Graph, Twitter Card, JSON-LD |
| L2 | Нет Sitemap.xml, robots.txt |
| L3 | Нет минификации CSS/JS |
| L4 | Нет lazy-loading изображений |
| L5 | Нет тёмной темы |
| L6 | Нет редактирования рейсов/машин |
| L7 | Нет подтверждения перед опасными действиями |
| L8 | Двойной scroll listener в JS |
| L9 | setTimeout 600ms перед редиректом |
| L10 | График выручки хрупкий (зависит от enum .name) |

---

# 7. ЗАКЛЮЧЕНИЕ И РЕКОМЕНДАЦИИ

## 7.1 Приоритеты исправлений

### 🔴 Первая очередь (безопасность и критические баги):
1. **Auth Bypass** — удалить или защитить URL-параметр в main.dart
2. **Пароли** — добавить хеширование (хотя бы SHA-256, лучше bcrypt)
3. **Тикеты** — исправить ключи (`numino_tickets` vs `tickets`)
4. **CSV-экспорт** — использовать правильный метод для CSV
5. **Firebase Auth Service** — реализовать или удалить заглушку

### 🟡 Вторая очередь (функциональность):
1. **XSS-санитайзер** — полноценная защита
2. **GPS-треки** — сохранять в SharedPreferences
3. **Удаление машин** — исправить индекс
4. **WASM MIME** — обновить start.bat/server.bat
5. **Деплой** — настроить Firebase hosting и .firebaserc

### 🟢 Третья очередь (UI/UX):
1. Минификация CSS/JS
2. Тёмная тема
3. Анимации для prefers-reduced-motion
4. Пагинация списков
5. focus-visible стили

## 7.2 Что уже исправлено

За время QA-сессии было исправлено 11 проблем:
1. Бесконечная загрузка админки (flutter_bootstrap.js)
2. Логаут на 3 экранах (owner, superadmin, driver)
3. Сохранение рейсов (saveTrips())
4. Сохранение тикетов (saveTickets())
5. Сохранение зарплаты (saveSalaryRules/Payments)
6. Лишний `</div>` в testimonials
7. 8 кнопок без `type="button"`
8. Добавлен `<main>` элемент
9. Honeypot в HTML контактной формы
10. Лейблы привязаны к инпутам (for/id)
11. Firebase hosting секция в firebase.json

---

## 🏁 ИТОГО

| Метрика | Значение |
|---------|---------|
| Всего файлов проанализировано | 30+ |
| Критических проблем (🔴) | 5 |
| Высоких проблем (🔴) | 10 |
| Средних проблем (🟡) | 12 |
| Низких проблем (🟢) | 10 |
| **Всего проблем** | **37** |
| Исправлено в процессе QA | 11 |
| Осталось исправить | 26 |

---

*Отчёт составлен QA-инженером. Для уточнений или повторного аудита — обращайтесь.*
